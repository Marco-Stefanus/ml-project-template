project:
  name: ml_project
  random_state: 42

data:
  raw_path: data/raw
  processed_path: data/processed
  target_column: target
  test_size: 0.2

model:
  name: baseline_model
  params: {}

training:
  epochs: 1
  batch_size: 32

logging:
  level: INFO
